#!/usr/bin/env bash
# Print the best currently enrolled Claude account lane for dispatch.
set -uo pipefail

SCRIPT_DIR=$(CDPATH='' cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
exec "$SCRIPT_DIR/ai-lanes" claude-pick "$@"
